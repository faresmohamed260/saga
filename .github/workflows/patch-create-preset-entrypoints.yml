name: Apply Create Preset Entrypoint Fix

on:
  push:
    branches:
      - studio/create-advanced-presets
    paths:
      - .github/workflows/patch-create-preset-entrypoints.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: studio/create-advanced-presets
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python scripts/patch_create_preset_entrypoints.py
      - run: |
          rm scripts/patch_create_preset_entrypoints.py
          rm .github/workflows/patch-create-preset-entrypoints.yml
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m "fix(studio): apply model presets on every Create entrypoint"
          git push origin HEAD:studio/create-advanced-presets
