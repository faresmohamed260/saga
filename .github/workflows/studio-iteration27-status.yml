name: Studio Iteration 27 Status

on:
  push:
    branches: [studio/video-gallery-ux]
    paths:
      - "scripts/studio_iteration27_status.py"
      - ".github/workflows/studio-iteration27-status.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: studio/video-gallery-ux
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python scripts/studio_iteration27_status.py
      - name: Commit Iteration 27 status
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/studio-ui-polish-checklist.md
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "docs: start Iteration 27 validation"
          git push origin HEAD:studio/video-gallery-ux
