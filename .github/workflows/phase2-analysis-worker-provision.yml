name: Phase 2 analysis worker provision

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Phase 2 worker provisioning is temporarily manual while credential handling is hardened."
