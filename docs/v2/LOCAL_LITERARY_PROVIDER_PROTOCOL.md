# Local Literary NLP Subprocess Protocol

Status: **EXPERIMENTAL PHASE-3B EXECUTION BOUNDARY**

This document defines the first v2-owned local-process boundary for literary NLP providers. It exists so the TypeScript analysis worker/evaluation stack can invoke local Python or other local model processes without making provider SDKs, model IDs, transport details, or Python package types part of S.A.G.A. application truth.

It does **not** adopt subprocess execution as the permanent winner over a loopback HTTP sidecar. The repository will compare operational simplicity after real local providers are exercised.

## Ownership

S.A.G.A. owns:

- request/response schema versions;
- normalized-input/configuration fingerprints;
- source offsets and structural locators;
- provider descriptors;
- evidence validation;
- retryability classification at the provider boundary;
- downstream canonicalization and persistence policy.

A local provider owns only model-specific inference and conversion into the returned provider-neutral evidence bundle.

## Protocol v1

Protocol identifier:

`SAGA local literary subprocess v1` / `saga-local-literary-subprocess-v1`

Each request launches one subprocess with `shell: false`.

- stdin: exactly one UTF-8 JSON request followed by `\n`;
- stdout: exactly one UTF-8 JSON response document;
- stderr: diagnostic-only, never parsed as evidence;
- process exit code `0`: stdout must contain a protocol response;
- non-zero exit: treated as a provider process crash/failure;
- request, stdout, stderr and execution time are bounded by the TypeScript caller.

The one-process-per-request lifecycle is intentionally simple for the first measured boundary. It may be replaced by a persistent loopback service if model startup cost or operational measurements justify it.

## Requests

Common request fields:

- `schemaVersion = saga-local-literary-request-v1`;
- `requestId` generated by the TypeScript caller;
- `protocolVersion = saga-local-literary-subprocess-v1`;
- `configurationFingerprint` — exact 64-character lowercase SHA-256-style fingerprint of provider/model/configuration state.

### `health`

The provider verifies that its configured artifacts/runtime are available and responds without exposing credentials, paths containing secrets, or source text.

### `analyze`

Adds:

- `normalizedInputFingerprint`;
- full normalized text for the bounded analysis request;
- section descriptors containing stable key, ordinal, kind, title, source locator and absolute Unicode code-point offsets.

`normalized_text` is deliberately omitted from each section descriptor because the request already carries the normalized source once.

## Responses

Common response fields:

- `schemaVersion = saga-local-literary-response-v1`;
- matching `requestId`;
- matching protocol/configuration fingerprints;
- provider descriptor `{ name, model, revision }` matching the caller's configured descriptor.

### `health`

Returns `kind = health` and `status = ok`.

### `analyze`

Returns `kind = analyze`, the exact normalized-input fingerprint, and a `LocalLiteraryEvidenceBundle` containing provider-neutral evidence families owned by v2:

- identity mentions;
- typed literary entities;
- quote/speaker evidence;
- literary event triggers;
- optional sentence-wide syntax-token evidence for providers that expose dependency structure.

The syntax layer is additive and optional so providers without dependency output remain protocol-compatible. When present, each syntax token contains exact source span/surface, lemma, paragraph/sentence/document token coordinates, POS tags, dependency relation and document-level syntactic head token ID. This layer exists to support deterministic grounding such as actor/patient attachment; it is evidence, not canonical narrative truth.

### `error`

A protocol-compliant provider may return `kind = error` with:

- bounded machine-readable `error.code`;
- explicit `error.retryable` boolean.

This is preferred to crashing when the provider can classify the failure itself.

## Source and evidence validation

The TypeScript boundary fails closed unless returned evidence satisfies all current invariants:

- configured provider descriptor matches the response and nested identity evidence;
- normalized-input fingerprint matches exactly;
- every source span uses absolute Unicode code-point offsets;
- every span is in range and reproduces the exact returned surface text;
- structural locators, when present, must match a containing normalized section;
- evidence IDs are non-empty and unique within each evidence family;
- enum-like mention/entity/boundary values are recognized;
- quote speaker span fields are either all present or all absent;
- event token/sentence/head IDs are non-negative safe integers;
- malformed or partial payload shapes fail closed.

When optional syntax-token evidence is present, the validator additionally requires:

- unique document token IDs;
- unique `(sentenceId, tokenIdWithinSentence)` coordinates;
- every syntactic head ID resolves to another returned syntax token (or the same token for a root/self-head representation);
- dependency heads stay within the same sentence;
- every returned event trigger maps to the same syntax token ID with identical source span, surface, lemma, sentence, dependency relation and head ID.

The validator builds the source code-point index once per analysis result so validating many spans does not repeatedly rescan the whole novel.

## Process security boundary

The caller does not inherit the worker environment wholesale. Only a small runtime allowlist is inherited by default (for example `PATH`, temporary/home/runtime variables and `VIRTUAL_ENV`). In particular, Supabase/B2 worker credentials and arbitrary `SAGA_*` variables are not inherited automatically.

Provider-specific environment values may be supplied deliberately by the caller. They are an explicit configuration surface, not ambient inheritance.

The subprocess is launched without a shell. Executable/arguments/environment values containing NUL are rejected.

Model inference is expected to work without network access after model artifacts are installed. Provider wrappers should use their ecosystem's offline flags/cache configuration where needed. This protocol is not itself a network sandbox.

## Failure classification

Examples of terminal/non-retryable boundary failures:

- invalid/mismatched protocol fields;
- mismatched provider/configuration/input fingerprints;
- malformed evidence;
- stdout/stderr/input limit violations;
- invalid JSON;
- missing executable or other persistent spawn configuration errors.

Examples of retryable failures:

- timeout;
- resource-temporary spawn errors such as `EAGAIN`/`ENOMEM`;
- abnormal provider exit where the process could not return a structured classification;
- a protocol `error` response explicitly marked retryable.

Downstream durable-job retry policy is still owned by the S.A.G.A. worker/database layer. This boundary only classifies provider failures for callers to consume.

## CI policy

Normal CI must remain model-light. The subprocess contract is tested with a tiny Node fixture process that exercises:

- health/analyze round trips;
- secret stripping;
- protocol/provider/fingerprint mismatch;
- exact source-span validation;
- optional syntax graph/head/event consistency validation;
- timeout and I/O limits;
- process crash/spawn failure;
- provider-reported retryable versus terminal errors.

No BookNLP, GLiNER, F-Coref, Qwen, GPU runtime, paid API, or Modal textual workload is downloaded/run by these tests.

## Adoption boundary

This Phase-3B boundary implements and validates local evidence transport only.

It does **not**:

- promote BookNLP or any other provider;
- make dependency relations canonical participants/events;
- create a new durable application analysis job/table;
- replace the current TypeScript orchestration owner;
- satisfy the private modern-fiction product qualification gate;
- authorize a Vercel deployment;
- authorize paid inference or Modal text analysis.

The syntax extension specifically enables the measured #214 dependency-aware event-grounding challenger. Participant attachment must still resolve through S.A.G.A.-owned identity/entity evidence and must be benchmarked separately from trigger detection.

A persistent loopback sidecar may still be compared against one-shot subprocess execution if real startup/resource measurements justify it.
